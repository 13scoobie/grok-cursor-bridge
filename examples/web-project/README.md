# web-project Example

A minimal example showing a different tech stack (web/API focused) from the main `sample-project`.

## Structure

- `.cursor/skills/api-guardian/SKILL.md`

This demonstrates that the bridge works for any kind of project — not just the iOS/SwiftUI style in the primary sample.

Try running the bridge while inside this folder to see how it handles a different naming/structure.
